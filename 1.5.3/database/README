Build and push all images to nexus:
$ mvn clean deploy -P release # For use in environments with populated databases

Build and push all images to nexus with dev context:
$ mvn clean deploy -P release,dev # For use in clean databases

Build target dir and its contents locally only:
$ mvn clean install

Build all images locally:
$ mvn clean install -P release # For use in environments with populated databases

Build all images locally with dev context:
$ mvn clean install -P release,dev # For use in clean databases
