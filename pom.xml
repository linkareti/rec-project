<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<artifactId>parent</artifactId>
		<groupId>com.linkare.maven</groupId>
		<version>1.0.4</version>
	</parent>
	<groupId>com.linkare</groupId>
	<artifactId>rec.web-api</artifactId>
	<name>rec.web-api</name>
	<version>1.1.1-SNAPSHOT</version>
  <properties>
    <keystore.path>/path/to/the/keystore</keystore.path>
    <keystore.password>Store password</keystore.password>
    <keystore.alias>Alias name</keystore.alias>
  </properties>
	<scm>
		<connection>scm:svn:https://svn.linkare.com/internal/rec-project/rec/branches/rel-web-api-1.1.x</connection>
		<developerConnection>scm:svn:https://svn.linkare.com/internal/rec-project/rec/branches/rel-web-api-1.1.x</developerConnection>
		<url>https://sventon.linkare.com/repos/svnroot/list/internal/rec-project/rec/branches/rel-web-api-1.1.x</url>
	</scm>
	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>3.0</version>
				<configuration>
					<source>1.6</source>
					<target>1.6</target>
				</configuration>
			</plugin>
			<plugin>
				<artifactId>maven-release-plugin</artifactId>
				<version>2.3.2</version>
				<configuration>
					<tagNameFormat>rel-web-api-@{project.version}</tagNameFormat>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>jaxb2-maven-plugin</artifactId>
				<version>1.6</version>
				<executions>
					<execution>
						<goals>
							<goal>schemagen</goal>
						</goals>
						<phase>generate-resources</phase>
						<configuration>
							<transformSchemas>
								<transformSchema>
									<uri>http://rec.linkare.com/client/config</uri>
									<toFile>rec-face-config.xsd</toFile>
								</transformSchema>
							</transformSchemas>
							<includes>
								<include>com/linkare/rec/web/config/*.java</include>
							</includes>
							<outputDirectory>${project.build.directory}/schemas</outputDirectory>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>build-helper-maven-plugin</artifactId>
				<version>1.7</version>
				<executions>
					<execution>
						<id>attach-rec-face-config-xsd</id>
						<phase>package</phase>
						<goals>
							<goal>attach-artifact</goal>
						</goals>
						<inherited>false</inherited>
						<configuration>
							<artifacts>
								<artifact>
									<file>${project.build.directory}/schemas/rec-face-config.xsd</file>
									<type>xsd</type>
								</artifact>
							</artifacts>
						</configuration>
					</execution>
				</executions>
			</plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-site-plugin</artifactId>
        <version>3.7.1</version>
      </plugin>
		</plugins>
	</build>
  <profiles>
    <profile>
      <id>linkare-release</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-jarsigner-plugin</artifactId>
            <version>3.0.0</version>
            <executions>
              <execution>
                <id>sign</id>
                <goals>
                  <goal>sign</goal>
                </goals>
              </execution>
            </executions>
            <configuration>
              <keystore>${keystore.path}</keystore>
              <alias>${keystore.alias}</alias>
              <storepass>${keystore.password}</storepass>
            </configuration>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>
